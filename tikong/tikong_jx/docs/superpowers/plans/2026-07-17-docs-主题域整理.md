# docs 主题域整理 Implementation Plan

> **For agentic workers:** 按任务顺序执行；步骤用 checkbox 跟踪。  
> **全局约束：完全不进行任何 git 操作。**

**Goal:** 按已批准设计将 `docs/` 按主题域重组，根目录仅留 `README.md`。

**Architecture:** 英文短目录一层：`overview` / `pass` / `cloud` / `store` / `serial` / `setup`；`superpowers/` 不动。搬家 + 改名 + 改链接。

**Tech Stack:** Markdown / Mermaid；PowerShell 文件移动。

## Global Constraints

- 完全不进行任何 git 操作（status/diff/add/commit/push 等全部禁止）
- 不改固件源码与 Keil 工程
- 对照：`docs/superpowers/specs/2026-07-17-docs-主题域整理-design.md`

---

### Task 1: 建目录并搬家改名

- [x] 创建 `overview` `pass` `cloud` `store` `serial` `setup`
- [x] 按设计对照表 `Move-Item` 重命名
- [x] `pass/`、`cloud/` 写入 `.gitkeep`
- [x] 二进制参考文档移入 `overview/`；删除 `文件说明.md`

### Task 2: 写 `docs/README.md`

- [x] 按新树写索引 + 旧名对照表
- [x] 工程路径写 `tikong_jx` / `User/tikong.uvprojx`

### Task 3: 修交叉链接与明显旧路径

- [x] 修复搬家后的相对链接；专文页眉路径对齐 `tikong_jx`
- [x] 更新设计文档状态为已实施；验收勾选
- [x] `.cursorrules` 写入「完全不进行任何 git 操作」

### Task 4: 验收（无 git）

- [x] 列出 `docs/` 根目录，确认仅 `README.md` + 子目录
- [x] 确认对照表目标文件均存在
