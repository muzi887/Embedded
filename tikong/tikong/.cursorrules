# Cursor 开发规则文件
# 为 Cursor AI 提供项目上下文和开发规范

## 项目概述
这是一个基于 STM32F407VET6 的嵌入式系统项目，使用 Keil MDK 开发环境。
主要功能包括：
- 串口通信（USART2/3/4/5）
- JSON 数据处理（cJSON）
- EEPROM 存储管理
- DS3231 实时时钟
- SHA1 加密算法
- 黑名单管理
- 数据解析和处理

## 代码规范

### C 语言规范
- 使用标准 C99 语法
- 函数命名：驼峰命名法（如 `parseSerialData`）
- 变量命名：小写下划线分隔（如 `received_data_len`）
- 宏定义：全大写（如 `MAX_RECEIVE_LEN`）
- 头文件保护：使用 `#ifndef` / `#define` / `#endif`

### 代码风格
- 使用 4 个空格缩进（不使用 Tab）
- 函数之间空一行
- 复杂逻辑添加注释说明
- 关键变量和函数添加注释

### 文件组织
- `USER/` - 入口（main）、板级初始化（board_init）、应用轮询（app_run）
- `app/` - 领域与协议（data_handler、黑名单、Live_data、cmd）
- `app/comm/` - 各串口通道主循环处理（PCProcess、G4GProcess、QRProcess 等）
- `drivers/` - 片外器件（I2C、EEPROM、DS3231）
- `board/` - 本板外设（HC595、ext_io、timer、floor_ctrl、malloc）
- `third_party/` - 第三方源码（cJSON、sha1）
- `SYSTEM/` - 延时、sys、UART 驱动层（不含业务 Process）
- `FWLIB/`、`CORE/` - 标准库与内核

## 开发注意事项

### 内存管理
- 注意栈溢出风险，避免大数组定义在栈上
- 使用动态内存分配时注意内存泄漏
- 缓冲区大小要合理，避免溢出

### 串口通信
- 注意数据接收缓冲区的边界检查
- 使用状态机处理串口数据接收
- 注意数据包的完整性和校验

### 中断处理
- 中断服务函数保持简短
- 避免在中断中调用阻塞函数
- 使用标志位进行中断与主循环通信

### 错误处理
- 函数返回值检查
- 指针使用前检查 NULL
- 数组访问前检查边界

## 常用 API

### 串口操作
- `USART_SendData(USART_TypeDef* USARTx, uint16_t Data)` - 发送单字节
- `USART_ReceiveData(USART_TypeDef* USARTx)` - 接收单字节

### 延时函数
- `delay_ms(uint32_t ms)` - 毫秒延时
- `delay_us(uint32_t us)` - 微秒延时

### EEPROM 操作
- `EEPROM_Read()` / `EEPROM_Write()` - 读写 EEPROM

### JSON 处理
- 使用 cJSON 库进行 JSON 解析和生成

## IDE / Microsoft C/C++（F12）
跨文件跳转依赖 **`compile_commands.json` + 活动配置 `Win32`（挂 compileCommands）**（见 `.vscode/settings.json`、`c_cpp_properties.json`）。若误选 **Template_Template**、用户级 **`C_Cpp.default.compilerPath` 盖住数据库**、或 Browse 陈旧，都会导致 F12 异常。步骤：**fix_code_index.cmd** → 状态栏选 **Win32** → **C/C++: Reset IntelliSense Database**；详见 **`IDE_INDEX.md`**。

## 调试建议
- 使用串口打印调试信息
- 注意观察编译警告
- 使用 Keil 调试器进行断点调试
- 检查内存使用情况

## AI 辅助开发提示
- 修改代码时注意保持与现有代码风格一致
- 新增功能时考虑内存限制和实时性要求
- 建议代码修改时先理解现有架构
- 嵌入式开发要考虑资源限制（RAM、Flash）
